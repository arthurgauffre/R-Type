/*
** EPITECH PROJECT, 2024
** R-Type
** File description:
** IManager
*/

class IManager {
public:
  /**
   * @brief Construct a new IManager object.
   */
  IManager();

  /**
   * @brief Destroy the IManager object.
   *
   * This is the destructor for the IManager interface. It is declared virtual to ensure that
   * the destructors of derived classes are called when an object is deleted through a pointer
   * to IManager.
   */
  ~IManager();

protected:
private:
};
